{% extends 'base.html.twig' %}

{% block title %}Modifier le profil{% endblock %}

{% block body %}
    <div id="margin-t1" class="container text-center">
        <div class="card w-75 mx-auto bg-transparent border-0 mx-auto">
            <div class="card-body mx-auto">
                <h1 class="h3 mb-3 fs-1 text-light">Modifier le profil</h1>

                {{ include('user/_form.html.twig', {'button_label': 'Valider'}) }}

                <a class="btn btn-primary mt-2 border-light mb-5" href="{{ path('user_index') }}">Retour au profil</a>
            </div>
        </div>
    </div>

{% endblock %}
