<form method="post" action="{{ path('admin_annonces_delete_profil', {'id': annonce.id}) }}" onsubmit="return confirm('Etes vous sûr de vouloir supprimer l'annonce ?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ annonce.id) }}">
    <button class="btn btn-danger mt-2 border-light">Supprimer</button>
</form>
