{% extends 'base.html.twig' %}

{% block title %}Modifier l'annonce{% endblock %}

{% block body %}
    <div id="margin-t" class="container">
        <h1 class="text-light  text-center">Modifier l'annonce</h1>
        <div class="row mt-4">
            <div class="col">
                {{ include('admin/annonces/_form.html.twig', {'button_label': 'Valider'}) }}
                {{ include('annonces/_delete_form.html.twig') }}
            </div> 
        </div>
        <a id="btn" class="btn btn-primary mt-2 mb-4 border-light"href="{{ path('admin_annonces_index') }}">retour à l'index</a>
    </div>
{% endblock %}
{% block javascripts %}
    <script src="{{ asset('assets/js/images.js') }}"></script>
{% endblock %}
